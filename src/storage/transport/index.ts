/**
 * Storage transports — open shard store (`ShardClient`) and gated
 * multi-user manifest store (`SharedSpaceClient`).
 *
 * `export *` form so the dts roll-up keeps the symbols.
 */

export * from "./ShardClient";
export * from "./SharedSpaceClient";
